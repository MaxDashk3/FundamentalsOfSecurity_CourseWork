@model Movies.Models.Hall

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Hall</h4>
    <hr />
    @{
        await Html.RenderPartialAsync("DetailsPartial", Model);
    }
</div>
<div>
    @if(User.IsInRole("Admins"))
    {
        <a asp-action="Edit" asp-route-id="Model?.Id">Edit</a>
        <span>|</span>
    }
    <a asp-action="Index">Back to List</a>
</div>
